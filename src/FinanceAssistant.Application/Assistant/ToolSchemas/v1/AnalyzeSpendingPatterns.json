{
  "name": "AnalyzeSpendingPatterns",
  "kind": "advice",
  "description": "Request read-only spending advice grounded in Application read results.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "year": { "type": "integer" },
      "month": { "type": "integer", "minimum": 1, "maximum": 12 }
    },
    "required": [ "year", "month" ]
  },
  "resultContract": {
    "observedFacts": "Facts from retrieved transactions and summaries only.",
    "recommendations": "Recommendations derived from observed facts.",
    "budgetSuggestions": "Budget ideas separated from facts.",
    "noDataReason": "Controlled reason when stored data is insufficient."
  }
}
