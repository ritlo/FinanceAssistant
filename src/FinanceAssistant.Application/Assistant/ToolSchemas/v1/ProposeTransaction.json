{
  "name": "ProposeTransaction",
  "kind": "writeProposal",
  "description": "Propose a transaction write for messages where the user spent, paid, bought, purchased, received income, or asked to add a transaction. Confirmation is required before persistence.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "amount": { "type": "number" },
      "description": { "type": "string" },
      "date": { "type": "string", "format": "date" },
      "transactionType": { "type": "string" },
      "categoryName": { "type": "string" }
    },
    "required": [ "amount", "description", "date" ]
  }
}
