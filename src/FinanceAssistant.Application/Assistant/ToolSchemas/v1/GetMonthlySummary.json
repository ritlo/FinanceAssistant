{
  "name": "GetMonthlySummary",
  "kind": "read",
  "description": "Read a monthly income and expense summary for questions like how much was spent this month or what the active month totals are.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "year": { "type": "integer" },
      "month": { "type": "integer", "minimum": 1, "maximum": 12 }
    },
    "required": [ "year", "month" ]
  }
}
