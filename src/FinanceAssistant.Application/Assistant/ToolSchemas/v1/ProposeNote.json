{
  "name": "ProposeNote",
  "kind": "writeProposal",
  "description": "Propose a note write. Confirmation is required before persistence.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "content": { "type": "string" }
    },
    "required": [ "content" ]
  }
}
